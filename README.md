# Install Python deps
```
pip install flask flask-cors opencv-python numpy

pip install ultralytics   # optional, enables phone detection
```

# Run backend
```
python proctor_server.py
```

# Open frontend in Chrome
```
open proctor.html
```
